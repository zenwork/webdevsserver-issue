{
    "name": "root",
    "private": true,
    "scripts": {
        "clean": "node_modules/.bin/lerna clean -y",
        "bootstrap": "node_modules/.bin/lerna bootstrap --strict",
        "bootstrap:hoist": "node_modules/.bin/lerna bootstrap --hoist --strict",
        "start": "web-dev-server --node-resolve --watch"
    },
    "devDependencies": {
        "@rollup/plugin-commonjs": "^16.0.0",
        "@rollup/plugin-node-resolve": "^10.0.0",
        "@web/dev-server": "^0.0.21",
        "@web/dev-server-esbuild": "^0.2.6",
        "@web/dev-server-rollup": "^0.2.11",
        "@web/test-runner": "^0.9.11",
        "@web/test-runner-cli": "^0.6.12",
        "@web/test-runner-junit-reporter": "^0.2.1",
        "@web/test-runner-playwright": "^0.6.6",
        "lerna": "^3.22.1"
    }
}
